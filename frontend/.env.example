# MusicTrack — Frontend env

# URL del backend de MusicTrack.
# Local:        http://localhost:3001
# Producción:   https://musictrack-api.vercel.app  (o como se llame el deploy del backend)
#
# Si está vacía, el frontend usa el mock local de src/data/products.js
# como fallback (útil para deploys del frontend sin backend).
NEXT_PUBLIC_API_URL=http://localhost:3001
